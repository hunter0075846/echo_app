# 产品需求文档（PRD）

## 产品名称：回响（Echo）

**版本**：1.1.0
**更新日期**：2026年8月15日
**文档状态**：已确认方向，待排期

---

## 变更记录

| 日期 | 版本 | 变更人 | 摘要 |
|------|------|--------|------|
| 2026-04-12 | 1.0 | hunter0075846 | 初稿 |
| 2026-05-03 | 1.0.1 | leo | 重写 4.1 用户账号与登录注册：1.0 实际为手机号+密码方案，将验证码登录/一键登录/微信登录/注销账号/二次验证统一移入第 6 章 Roadmap；新增 4.1.5 安全与可靠性子节描述 P0 加固成果（防爆破限流、登录态加密存储、自动登出等）；同步修订 5.2 "注销账号"措辞，使之与现状自洽 |
| 2026-08-15 | 1.1.0 | leo | 全量代码盘点后重写：① 产品定位扩展为「话题广场 + 熟人小群 + 个人 AI 接入」，OpenClaw 桥接、AI 助手、自建 Agent（BYOK）、好友/私聊从"代码有 PRD 无"转为正式功能（第 3.6-3.9 节）；② 1.0 遗留 P0 缺口（话题转发链路、匿名投票、群回忆时间线、举报机制）确认保留并列入 v1.1 排期（第 3.2/3.3/3.10 节）；③ 每个功能模块增加"实现状态"标注；④ 目标用户扩展为双人群；⑤ 更新 Roadmap |

---

## 1. 产品概述

### 1.1 产品定位

**回响（Echo）** 是一款 **AI 深度嵌入社交关系** 的 App，由三层构成：

- **内容层**：话题广场。用户粘贴链接或上传图片，AI 自动生成话题卡片发布到广场，所有人可浏览讨论。
- **关系层**：好友、私聊、10-30 人熟人小群。群内低压力讨论（匿名发言、无已读回执），精彩片段自动沉淀为回忆时间线。
- **AI 层（差异化核心）**：用户不仅能用平台提供的 AI 助手，还能**把自己的 AI 接进社交关系**——通过 OpenClaw 将跑在自己电脑上的 AI agent 接入 App（私聊 + 挂进群当 bot），或通过自建 Agent 接入任意 OpenAI 兼容服务（BYOK，用户自带 key）。

### 1.2 核心价值

- **总有话题可聊**：每天可发布 10 条话题，广场实时更新，AI 自动起标题、写描述、做审核。
- **表达无压力**：匿名发言、匿名投票、匿名短评；无已读回执、无在线状态。
- **回忆自动沉淀**：群聊精彩片段（话题讨论、投票结果、高光发言）自动保存为时间线，永久可回溯。
- **AI 进关系链**：平台 AI 助手（小安）随叫随到；用户自己的 OpenClaw / 自建 Agent 可以作为私聊对象，也可以挂进群里参与讨论，由编排器自动调度。

### 1.3 目标用户

**人群 A（大众社交用户）**：
- 年龄 18-30 岁，拥有至少一个活跃的熟人小群（死党群、闺蜜群、家庭群、室友群）。
- 痛点：群聊冷场、话题枯竭、想讨论热点但不想公开、怀念有趣讨论但找不到记录。
- **i 人友好**：提供匿名、跳过选项、无社交压力显示。

**人群 B（AI 重度用户 / 技术用户）**：
- 自己在电脑上跑 OpenClaw 或有常用的大模型服务（OpenAI 兼容）。
- 痛点：自己的 AI agent 被困在 CLI/网页里，无法随时随地对话，更无法带进朋友和群的场景。
- 诉求：BYOK 自己承担模型成本、数据走自己的服务、agent 能进入私聊和群聊。

### 1.4 设计原则（6 条）

1. **永远有「跳过」选项**：任何 AI 主动发起的互动（话题卡片、投票邀请、匿名发言提醒）都提供"跳过"，且不产生负面记录。
2. **不显示社交压力信号**：无已读回执（人与人之间）、无在线状态、无"正在输入"（人与人之间）。AI 通道（OpenClaw/Agent）的 typing 与回执属于机器协议，不在此限。
3. **AI 语气温和、商量口吻**：用"要不要试试？"而不是"你应该"。
4. **群内数据私密**：所有群内讨论、回忆内容仅群成员可见，删除操作不公开执行者。
5. **匿名可追溯（仅系统）**：后台记录匿名发言的真实用户，用于处理违规举报，不向任何群成员公开。
6. **AI 身份透明**：所有 AI 发出的消息（小安、OpenClaw bot、Agent）必须有明确标识，不伪装成人。

---

## 2. 功能范围（v1.1）

实现状态图例：✅ 已上线　🟡 半成品/待补齐　❌ 未开发

| 功能模块 | 描述 | 状态 | v1.1 优先级 |
|---------|------|------|------|
| **业务功能** | | | |
| 热门话题广场 | 话题列表（最新/最热）、搜索、详情、公共评论、点赞 | ✅ | — |
| 用户生成话题 | 粘贴链接/上传图片，AI 生成标题描述 + AI 审核，每日 10 条配额 | ✅ | — |
| 转发话题到群 | 话题卡片进群消息流、群内讨论页 | 🟡 链路断裂（App 端对话框 TODO，卡片无 API 读写） | **P0** |
| 群内匿名投票 | AI 生成选项、单票、实时比例不记名 | ❌ 仅 schema + `generateVoteOptions` 已备好 | **P0** |
| 群回忆时间线 | 自动抓取高光片段，卡片流，仅群成员可见 | ❌ 页面全 mock，`Memory` 模型已建 | **P0** |
| 匿名发言 | 群消息 `isAnonymous` 开关，每日 3 条配额 | ✅（简化版，无 AI 代发） | P1 增强 |
| 举报与风控 | 举报话题/评论/消息，3 次自动隐藏，人工复核 | ❌ 仅按钮 + TODO | **P0** |
| AI 助手小安 | 私聊 SSE 流式对话，群内入口，回复可转发回群 | ✅ | — |
| OpenClaw 桥接 | 个人 AI agent 接入 App：私聊 + 群 bot + 编排调度 | ✅ | P1 打磨 |
| 自建 Agent（BYOK） | 用户自配 baseURL/apiKey/model，私聊对话 | 🟡 代码完成 90%，未挂路由无入口 | **P0 上线** |
| 好友与私聊 | 好友申请/邀请码/扫码，会话与私聊 | ✅ | — |
| 冷启动辅助 | 新群预置话题、拉人解锁历史话题 | ❌ | P1 |
| **基础能力** | | | |
| 登录注册 | 手机号+密码，JWT，防爆破限流，登录态加密存储 | ✅ | — |
| 版本检查与升级 | 启动检查、强制/非强制升级、APK 下载 | ✅ | — |
| 关于页面 | 版本、协议、隐私政策 | ✅ | — |
| 群管理 | 建群、邀请码、成员管理、转让群主、解散 | ✅ | — |
| 系统设置 | 通知/隐私/通用设置 | ❌ | P1 |

---

## 3. 详细功能需求

### 3.1 热门话题广场 ✅

（与 v1.0.1 一致，已实现。要点：）

- 广场列表：「最新」（默认）/「最热」双 Tab，搜索，分页加载。
- 发话题：粘贴链接（AI 抓取标题/描述/图）或上传图片（AI 生成描述）；每日 10 条，0 点重置；AI 审核命中则拒绝发布。
- 话题详情：标题、图、AI 描述、公共评论区（点赞、楼中楼回复、按热度/时间排序）。
- 发布者可删除自己的话题（级联删评论）。

### 3.2 转发话题到群 + 群内讨论区 🟡→P0

**现状**：后端 `GroupTopicCard` 模型、`topic_card` 消息类型已就绪；App 端「转发到群」对话框为 TODO，话题卡片无任何 API 读写。**v1.1 需补齐完整链路：**

1. 广场/详情页「转发到群」→ 群选择器 → 可编辑引导语（默认"大家来聊聊这个话题吧！"）→ 发送。
2. 群消息流出现话题卡片：标题、引导语、"已有 X 人参与"、「加入讨论」「跳过」按钮；48 小时后折叠为灰色提示，可重新展开。
3. 讨论页（仅本群成员可见）：话题信息 + 匿名投票区 + 匿名短评区 + 公开聊天区（与群消息流双向同步）。
4. 匿名投票/短评不进群消息流，但计入回忆时间线。

### 3.3 匿名投票 ❌→P0

**现状**：`Vote`/`AnonymousMessage`（内嵌投票选项）模型已建，`generateVoteOptions` 已实现未调用。**v1.1 需求：**

- 话题讨论页内：AI 基于话题生成 2-4 个选项，用户可修改；每人一票，可改投；实时显示比例，不显示谁投。
- 「有人说」匿名消息可附带简单投票（是/否 或 A/B）。
- 投票最终结果自动进入回忆时间线。

### 3.4 群回忆时间线 ❌→P0

**现状**：`Memory` 模型已建，App 页面全为本地 mock。**v1.1 需求：**

- 群聊页顶部固定入口。
- 自动抓取规则（与 v1.0.1 一致）：话题首次转发、投票结果、匿名短评获赞≥3、公开发言≥20字、图片/表情消息、手动「加入回忆」、高互动匿名发言。
- 卡片流按时间倒序，点击可跳转原始消息位置。
- 隐私：仅群成员可见；成员可删除"涉及我的回忆"（无删除记录）；群主可删任意卡片（二次确认）。
- 冷启动占位文案。

### 3.5 匿名发言 ✅（P1 增强）

**已实现**：群消息 `isAnonymous` 开关、"匿名发言中" UI、每日 3 条配额、真实身份后台可溯。

**P1 增强项**（对齐 v1.0.1 的「有人说」）：🎭 入口图标、"有人说："展示样式、附带简单投票、「我也想说」快捷按钮、敏感词二次确认。

### 3.6 AI 助手小安 ✅

- 私聊 SSE 流式对话，cursor 分页历史。
- 群聊列表置顶卡片入口；群内可唤起。
- 助手回复可一键转发回群（`agent_quote` 卡片，标注 AI 来源）。
- 场景：推荐热门话题、回答关于群聊的问题、（v1.2）群聊氛围分析、回忆总结。

### 3.7 OpenClaw 桥接 ✅

**定位**：让用户跑在自己电脑上的 OpenClaw agent 成为回响里的"远程大脑"。

- 连接管理：App 生成 token → 引导用户 `curl ... | bash` 安装插件 → 插件通过 WebSocket 长连。
- 私聊：App ↔ OpenClaw 双向消息，delivered/read 回执，多端广播。
- 群 Bot：OpenClaw 连接可挂入群（多对一），由"小安"编排器调度——@提及优先，JSON mode 决策派发，3 秒冷却。
- 协议：心跳 15s/超时 50s、sync 补发未送达、platformMsgId 去重、typing。
- **已知限制**：进程内连接表，单实例部署；上集群需换 Redis pubsub（记入 Roadmap）。
- **P1 打磨**：插件协议变更时的旧用户升级提醒。

### 3.8 自建 Agent（BYOK）🟡→P0 上线

**现状**：后端 CRUD + 聊天 API 完整，App 三个页面（列表/创建/聊天）已完成但未挂路由。**v1.1 上线需求：**

1. App 挂路由 + 「我的」页入口。
2. 创建 Agent：名称、头像、baseURL、apiKey、model、system prompt；key 本地安全存储 + 服务端加密落库，永不回显。
3. 私聊界面复用助手聊天页组件；标识"此 Agent 使用你自己的模型服务"。
4. 与平台助手并存：小安走平台配额（低成本模型），Agent 走用户自己的 key，平台零成本。

### 3.9 好友与私聊 ✅

- 好友：申请/接受/拒绝、邀请码、扫码添加、好友通知。
- 私聊：会话列表、消息收发。
- **定位**：私聊是人与人、人与 AI（小安/OpenClaw/Agent）的统一载体；不做陌生人社交。

### 3.10 举报与内容风控 ❌→P0

**现状**：发布侧 AI 审核已覆盖话题/评论/群消息（审核失败默认放行）；举报仅有按钮 + TODO。**v1.1 需求：**

- 举报入口：长按话题/评论/群消息/匿名消息 → 选择理由 → 提交。
- 同一内容被举报 3 次自动隐藏，进入待复核队列。
- 匿名消息举报后：后台查真实发送者，违规则删消息 + 警告/封禁，处理结果不向举报人和群成员公开身份。
- 运营侧：最简复核界面（可先用 Prisma Studio + 约定字段过渡，不做独立后台）。

### 3.11 冷启动辅助 ❌（P1）

- 新群满 3 人：AI 自动发 2 张当日热门话题卡片 + 引导语。
- 拉人解锁：群主每邀请 1 位新注册用户，解锁一条 7 日内最热话题，可发送到群（附公共评论精华）。
- ~~微信小程序尝鲜版~~：无限期延后（v1.1 明确不做）。

### 3.12 群管理基础功能 ✅

（与 v1.0.1 一致，已实现：建群、邀请码 24h/10 人、移除成员、转让群主、退出/解散、群设置页。管理员角色仍 P1。）

---

## 4. 基础能力与系统功能

### 4.1 用户账号与登录注册 ✅

（与 v1.0.1 的 4.1 节完全一致：手机号+密码、JWT 30 天、防爆破限流、登录态加密存储、401 自动登出、主动登出吊销 token。）

### 4.2 版本检查与升级 ✅

（与 v1.0.1 一致，已实现：启动静默检查、强制/非强制弹窗、APK 下载。）

### 4.3 关于页面 ✅

（已实现：版本号、用户协议、隐私政策。）

### 4.4 系统设置 ❌（P1）

| 设置项 | 描述 | 优先级 |
|--------|------|--------|
| 账号与安全 | 修改密码、注销账号 | P1 |
| 通知设置 | 全局开关、按群设置 | P1 |
| 隐私设置 | 手机号可搜索、AI 使用聊天记录优化推荐开关 | P1 |
| 通用设置 | 深色模式、清理缓存 | P1 |

### 4.5 密钥与成本纪律

- 平台 AI 调用默认走低成本模型（当前 deepseek），新模型/新调用点设计优先评估成本。
- 用户 API key（Agent/OpenAI 兼容服务）加密落库、不回显、不进日志。
- 密钥、token 不进代码、不进 commit。

---

## 5. 非功能需求

### 5.1 性能
- 广场列表加载 < 1s；群消息发送延迟 < 500ms；回忆时间线分页无限滚动。
- AI 生成（标题/描述/审核）不阻塞主流程，可异步兜底。

### 5.2 安全与隐私
- 传输 TLS；匿名映射仅审计可查且需审批。
- AI 通道消息明确标识机器身份。
- 注销账号能力 v1.1 提供（个人数据删除；群聊记录匿名化处理）。

### 5.3 兼容性
- Android 8+（主打），iOS 13+（后续），Web 预览仅限开发调试。

---

## 6. Roadmap

| 版本 | 内容 |
|------|------|
| **v1.1（当前排期）** | 补齐主线：话题转发链路 + 群内讨论页、匿名投票、群回忆时间线、举报与风控；自建 Agent 挂路由上线；账号注销；匿名发言增强（🎭/有人说样式） |
| v1.2 | 冷启动辅助（新群预置话题、拉人解锁）；系统设置完整版；登录方式扩展（验证码/微信）；管理员角色；AI 话题洞察（群内观点总结）；群回忆高光周报 |
| v1.3 | AI 下个话题推荐；匿名表情回应；OpenClaw 集群化（Redis pubsub）；高光合集视频；群记忆地图 |

---

## 7. 附录：关键术语表

| 术语 | 定义 |
|------|------|
| 话题广场 | 展示所有用户生成话题的页面 |
| 话题卡片 | 转发到群聊后出现在消息流中的交互卡片 |
| 群专属话题讨论页 | 群内点击话题卡片后进入的独立页面（投票 + 匿名短评 + 公开聊天） |
| 回忆时间线 | 自动生成的群聊历史精彩片段卡片流 |
| 「有人说」 | 匿名发言的展示样式，消息显示为"有人说" |
| 小安 | 平台 AI 助手 |
| OpenClaw | 外部 CLI AI agent，通过插件桥接进回响（私聊 + 群 bot） |
| 自建 Agent / BYOK | 用户自带 OpenAI 兼容服务（baseURL/apiKey/model）创建的私聊 AI |
| 编排器 | 决定群消息派发给哪个 OpenClaw bot 的调度逻辑（@提及优先，3s 冷却） |

---

**文档结束**
